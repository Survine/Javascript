console.log(Math.abs(-5)); // Converts +ve / -ve integer values to positive


console.log(Math.round(4.3)); // Converts decimal to nearest integer


console.log(Math.ceil(4.2)); // Converts decimal to upper integer


console.log(Math.floor(4.9)); // Converts decimal to lower integer


console.log(Math.min(4, 3, 6, 8)); // Finds the minimum value


console.log(Math.max(4, 3, 6, 8)); // Finds the maximum value


console.log(Math.random()); // Generates a random number between 0 and 1


//To make it work like python random
let min = 20;
let max = 30;
console.log(Math.floor(Math.random() * (max - min + 1) + min)); 


